

| nth node from the end | if ( fast == null ) {<br><br>// if n == length just remove head move one step<br><br>return head.next;<br><br>} |
| --------------------- | --------------------------------------------------------------------------------------------------------------- |
| k lists merge         | its direct just loop and add node                                                                               |
